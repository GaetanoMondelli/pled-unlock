# API Simulation Toggle
# Set to true to use API-based simulation instead of local simulation
NEXT_PUBLIC_USE_API_SIMULATION=false

# API Configuration
# API endpoint (defaults to current host if not set)
NEXT_PUBLIC_API_BASE_URL=

# Existing environment variables...
NEXT_PUBLIC_ENABLE_LLM_RULES=true
NEXT_PUBLIC_LAZY_RULE_MATCHING=false